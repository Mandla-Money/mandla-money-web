# Mandla Money company website

Marketing site. See [deployment.md](./deployment.md).

```bash
npm ci
npm run dev   # http://localhost:3009
```

Deploy: merge to **`staging`** → builds `staging-latest` → staging VPS (`company-website`).
